// TODO: Import Week type from types/week

export function calculatePicksOpenCloseUtc(_games: any[]) {
  // TODO: Calculate picksOpenUtc (3 days before first game), picksCloseUtc (1 hour before first game)
  return { picksOpenUtc: '', picksCloseUtc: '' };
}

export async function ensureWeekExists(_weekParams: any) {
  // TODO: Check if week exists, create if not, and return week
  return null;
}
